meta {
  name: Change Password
  type: http
  seq: 1
}

post {
  url: {{GRADELOOP_API_URL}}/auth/change-password
  body: json
  auth: none
}

body:json {
  {
    "currentPassword": "temp-password-here",
    "newPassword": "new-secure-password"
  }
}
