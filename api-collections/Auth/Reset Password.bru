meta {
  name: Reset Password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "REPLACE_WITH_ACTUAL_TOKEN",
    "newPassword": "newpassword123"
  }
}
