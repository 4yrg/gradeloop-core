meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{GRADELOOP_API_URL}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "dasun.wickr@gmail.com",
    "password": "temp-password-from-logs"
  }
}

vars:pre-request {
  GRADELOOP_API_URL: http://localhost:8000
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("authToken", res.body.token);
  }
}
