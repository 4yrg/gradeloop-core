services:
  authz-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: authz-service
    ports:
      - "4001:4001"
    environment:
      - PORT=4001
      - DATABASE_URL=/app/data/authz.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
