meta {
  name: Create Permission
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/internal/authz/permissions
  body: json
  auth: none
}

body:json {
  {
    "name": "course.create",
    "resource": "course",
    "action": "create",
    "description": "Can create new courses"
  }
}
