meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: json
  auth: none
}

body:json {
  {
    "refresh_token": "PASTE_REFRESH_TOKEN_HERE"
  }
}

assert {
  res.status: eq 200
}
