meta {
  name: Reset Password
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "your-reset-token-here",
    "new_password": "newpassword123"
  }
}
