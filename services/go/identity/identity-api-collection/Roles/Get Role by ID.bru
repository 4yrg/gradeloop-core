meta {
  name: Get Role by ID
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/{{api_version}}/roles/role-uuid-1
  body: none
  auth: none
}

params:path {
  id: role-uuid-1
}

docs {
  # Get Role by ID

  Retrieves a specific role by its ID.

  ## Path Parameters
  - `id`: The unique identifier of the role

  ## Response
  Returns the role object with all its details, including name, description, and permissions.
}
