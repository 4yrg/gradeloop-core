services:
  identity-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: identity-service
    ports:
      - "4003:4003"
    environment:
      - PORT=4003
      - DATABASE_URL=/app/data/identity.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
