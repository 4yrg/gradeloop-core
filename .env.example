# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gradeloop

# Google OAuth (Optional - for OAuth login)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# NextAuth
NEXTAUTH_SECRET=changeme123-please-use-a-secure-random-string
NEXTAUTH_URL=http://localhost:3000

# Auth Service (for local dev, use localhost; for Docker, use http://traefik/api/auth)
AUTH_SERVICE_URL=http://localhost:8080/api/auth

# JWT Secret for Auth Service
JWT_SECRET=supersecretkey-please-use-a-secure-random-string
