# name: "Lint Code"

# on:
#   push:
#     branches:
#       - main
#       - develop
#   pull_request:
#     branches:
#       - main
#       - develop

# jobs:
#   lint-code:
#     name: Lint - ${{ matrix.linter.name }}
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         include:
#           # --- Add an entry for each service you want to lint ---
#           - linter:
#               name: "Auth Service"
#               path: "./auth-service" # UPDATE THIS PATH
#               language: "go"
#               version: "1.21"
#           - linter:
#               name: "ACAFS Service"
#               path: "./acafs-service" # UPDATE THIS PATH
#               language: "python"
#               version: "3.11"
#           - linter:
#               name: "Frontend"
#               path: "./frontend" # UPDATE THIS PATH
#               language: "node"
#               version: "20.x"

#     steps:
#       - name: Checkout repository code
#         uses: actions/checkout@v4

#       # Linter for Go services
#       - name: Run golangci-lint
#         if: matrix.linter.language == 'go'
#         uses: golangci/golangci-lint-action@v4
#         with:
#           version: v1.58 # Or the version you prefer
#           working-directory: ${{ matrix.linter.path }}

#       # Linter for Python services
#       - name: Run Flake8
#         if: matrix.linter.language == 'python'
#         run: |
#           pip install flake8
#           flake8 .
#         working-directory: ${{ matrix.linter.path }}

#       # Linter for Node.js/TypeScript services
#       - name: Run ESLint
#         if: matrix.linter.language == 'node'
#         run: |
#           npm ci
#           npm run lint
#         working-directory: ${{ matrix.linter.path }}
